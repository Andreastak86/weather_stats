version: "3.9"

services:
  backend:
    build: ./backend
    container_name: weather-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: weather-frontend
    ports:
      - "8080:80"
